# --- Firebase (Google Sign-in + Firestore) ---
# Firebase Console → Project settings → Your apps → Web app
# Project: harmony-home-95c3b (519262340053)
# Authentication → Sign-in method → Google → Enable
# Authorized domains: localhost + domena produkcyjna
VITE_FIREBASE_API_KEY=AIzaSyDx6h_kp8gWfY3CSHbWYuO75X94m0c7GH8
VITE_FIREBASE_APP_ID=1:519262340053:web:899694e8cc03b85243335c
VITE_FIREBASE_AUTH_DOMAIN=harmony-home-95c3b.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=harmony-home-95c3b
VITE_FIREBASE_STORAGE_BUCKET=harmony-home-95c3b.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=519262340053

# --- Google OAuth (opcjonalnie — trwały refresh token na serwerze) ---
# Bez tego kalendarz sync działa przez Firebase (przycisk u góry w Ustawieniach).
# Google Cloud Console → projekt harmony-home-95c3b → Credentials → OAuth 2.0 Client
# Redirect URI: http://localhost:3000/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Losowy sekret: openssl rand -base64 32
AUTH_SECRET=
# Lokalnie: http://localhost:3000  |  Vercel: https://twoja-app.vercel.app
APP_URL=http://localhost:3000

# --- Diagnostyka deployu (Vercel) ---
# Losowy sekret do /api/debug?token=...
# DEBUG_SECRET=
# Pokaż stack trace na stronie błędu (preview/dev): 1 = włączone
# DEBUG_ERRORS=1

# --- Persystencja tokenów (Vercel: dodaj Redis/KV z marketplace) ---
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# --- Opcjonalny fallback iCal (bez OAuth) ---
GOOGLE_CALENDAR_ICAL_URL=

# Stas — tylko gdy nie używasz OAuth:
# STAS_CAL_ZAJECIA_ICAL_URL=
# STAS_CAL_WYDARZENIA_ICAL_URL=
# STAS_CAL_ZNAJOMI_ICAL_URL=
